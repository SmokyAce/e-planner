import React from 'react';

export const NotFound = () => {
    return (
        <div style={{ textAlign: 'center' }}>
            <img src='/img/404.gif' />
        </div>
    );
};

NotFound.propTypes = {};

export default NotFound;
