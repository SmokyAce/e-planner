import { defineMessages } from 'react-intl';

export default defineMessages({
    delete_event_desc: {
        id            : 'app.events.delete-event-description',
        defaultMessage: 'Do you really want delete event ?'
    }
});
